% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heckman_flex.R
\name{heckman_flex}
\alias{heckman_flex}
\title{Extended Heckman Selection Model}
\usage{
heckman_flex(
  formula,
  data,
  method = c("ml", "twostep", "twostep_robust", "semipar"),
  selection_type = c("binary", "ordered", "multinomial"),
  error_dist = c("normal", "t", "copula"),
  copula_family = c("gaussian", "frank", "clayton", "gumbel"),
  heteroscedastic = NULL,
  vcov = c("standard", "robust", "bootstrap"),
  latex = FALSE,
  ...
)
}
\arguments{
\item{formula}{Multi-part formula: \code{outcome | selection ~ X_outcome | X_selection}.}

\item{data}{A data frame.}

\item{method}{Estimation method: \code{"ml"}, \code{"twostep"},
\code{"twostep_robust"} (Lee), \code{"semipar"}.}

\item{selection_type}{\code{"binary"}, \code{"ordered"}, \code{"multinomial"}.}

\item{error_dist}{\code{"normal"}, \code{"t"}, \code{"copula"}.}

\item{copula_family}{\code{"gaussian"}, \code{"frank"}, \code{"clayton"}, \code{"gumbel"}.}

\item{heteroscedastic}{Formula for heteroscedasticity (optional).}

\item{vcov}{\code{"standard"}, \code{"robust"}, \code{"bootstrap"}.}

\item{latex}{If \code{TRUE}, prints a LaTeX table of results. Default: \code{FALSE}.}

\item{...}{Additional arguments passed to the optimiser.}
}
\value{
Object of class \code{c("heckman_flex", "ecoflex")}.
}
\description{
Estimate Heckman-type selection models with multiple estimation methods
and flexible error distributions.
}
\examples{
\donttest{
set.seed(42)
n <- 500
x1 <- rnorm(n); z <- rnorm(n)
s <- as.integer(0.3 * x1 + 0.5 * z + rnorm(n) > 0)
y <- ifelse(s == 1, 1 + 2 * x1 + rnorm(n), 0)
df <- data.frame(y = y, s = s, x1 = x1, z = z)
m <- heckman_flex(y | s ~ x1 | x1 + z, data = df, method = "twostep")
summary(m)
}
}
