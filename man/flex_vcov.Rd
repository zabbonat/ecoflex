% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcov_methods.R
\name{flex_vcov}
\alias{flex_vcov}
\title{Flexible variance-covariance matrix for ecoflex models}
\usage{
flex_vcov(
  object,
  type = "sandwich",
  cluster = NULL,
  R = 500,
  parallel = FALSE,
  cores = 2
)
}
\arguments{
\item{object}{An ecoflex model object}

\item{type}{Type of matrix: \code{"hessian"}, \code{"opg"}, \code{"sandwich"},
\code{"cluster"}, \code{"bootstrap"}}

\item{cluster}{Cluster variable (vector). Required if \code{type = "cluster"}.}

\item{R}{Number of bootstrap replications. Default: 500.}

\item{parallel}{Use parallel computation for bootstrap. Default: FALSE.}

\item{cores}{Number of cores for parallelisation. Default: 2.}
}
\value{
A k x k variance-covariance matrix
}
\description{
Compute variance-covariance matrices using different methods: inverse Hessian,
outer product of gradients (OPG), Huber-White sandwich, clustered, or bootstrap.
}
\examples{
\dontrun{
m <- hurdle_flex(y ~ x1 + x2, data = df)
V_sand <- flex_vcov(m, type = "sandwich")
V_cl   <- flex_vcov(m, type = "cluster", cluster = df$group)
V_boot <- flex_vcov(m, type = "bootstrap", R = 1000)

# Use with summary:
summary(m, vcov = V_cl)
}
}
