% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel_nl.R
\name{panel_nl}
\alias{panel_nl}
\title{Nonlinear Panel Data with Flexible Standard Errors}
\usage{
panel_nl(
  formula,
  data,
  id,
  time,
  family = c("poisson", "negbin", "logit", "probit", "gamma"),
  effect = c("individual", "time", "twoways"),
  model = c("fe", "re", "correlated_re"),
  vcov = c("standard", "robust", "cluster", "driscoll_kraay", "bootstrap"),
  cluster = NULL,
  DK_bandwidth = NULL,
  latex = FALSE,
  ...
)
}
\arguments{
\item{formula}{Formula: \code{outcome ~ regressors}.}

\item{data}{Panel data in long format.}

\item{id}{Name of unit identifier variable.}

\item{time}{Name of time variable.}

\item{family}{\code{"poisson"}, \code{"negbin"}, \code{"logit"},
\code{"probit"}, \code{"gamma"}.}

\item{effect}{\code{"individual"}, \code{"time"}, \code{"twoways"}.}

\item{model}{\code{"fe"}, \code{"re"}, \code{"correlated_re"} (Mundlak/Chamberlain).}

\item{vcov}{\code{"standard"}, \code{"robust"}, \code{"cluster"},
\code{"driscoll_kraay"}, \code{"bootstrap"}.}

\item{cluster}{Cluster variable (default: id).}

\item{DK_bandwidth}{Driscoll-Kraay bandwidth.}

\item{latex}{If \code{TRUE}, prints a LaTeX table. Default: \code{FALSE}.}

\item{...}{Additional arguments.}
}
\value{
Object of class \code{c("panel_nl", "ecoflex")}.
}
\description{
Estimate nonlinear panel models (Poisson, NegBin, Logit, Probit, Gamma)
with fixed effects, random effects, or correlated random effects.
}
\examples{
\donttest{
set.seed(42)
panel <- expand.grid(id = 1:30, time = 1:5)
panel$x <- rnorm(nrow(panel))
panel$y <- rpois(nrow(panel), exp(0.5 + 0.3 * panel$x))
m <- panel_nl(y ~ x, data = panel, id = "id", time = "time",
              family = "poisson", model = "fe")
summary(m)
}
}
